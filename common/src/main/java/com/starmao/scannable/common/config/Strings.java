package com.starmao.scannable.common.config;

import net.minecraft.network.chat.Component;
import net.minecraft.network.chat.MutableComponent;

import static net.minecraft.ChatFormatting.*;

/**
 * Central registry of pre-built text components and translatable message helpers.
 * <p>All messages use Minecraft's translation system ({@code translatable})
 * for full i18n support via language JSON files.
 */
public final class Strings {
